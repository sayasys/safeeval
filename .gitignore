# dependencies
node_modules/
.pnp
.pnp.js

# environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel

# secrets -- the PAT lives at the workspace root (one level above the repo), but
# this guard ensures it can never be committed even if accidentally placed inside.
.pat
.pat.*
PAT-SETUP.md
